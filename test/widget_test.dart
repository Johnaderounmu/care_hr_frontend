// This is a basic example Flutter widget test.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Simple test to verify testing works', () {
    expect(1 + 1, equals(2));
  });
}
